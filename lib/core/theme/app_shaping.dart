import 'package:flutter/material.dart';

final class AppShaping {

  AppShaping._();

  static const radius8 = Radius.circular(8);
  static const radius16 = Radius.circular(16);
  static const radius24 = Radius.circular(24);

  static const borderRadius8 = BorderRadius.all(radius8);
  static const borderRadius16 = BorderRadius.all(radius16);
  static const borderRadius24 = BorderRadius.all(radius24);

}